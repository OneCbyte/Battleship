#pragma once
#include "game.h"

void output(std::vector<std::vector<CellState>> gamefield) {
	for (const std::vector<std::vector<CellState>> row : gamefield) {
		for (const std::vector<std::vector<CellState>> row : gamefield)
	}
}